# No --version flag, no K6_DOCS_VERSION env.
# DetectK6Version reads go.k6.io/k6 from build info.
# In the test binary, dependency info is not embedded,
# so auto-detection fails with a clear error.
env K6_DOCS_CACHE_DIR=$WORK/cache
! exec k6-docs
stderr 'dependency not found in build info'
