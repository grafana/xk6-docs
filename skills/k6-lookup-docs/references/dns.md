# k6/x/dns

| I need to... | Path |
|---|---|
| DNS resolution API | `javascript-api k6-x-dns` |
| `resolve()` (custom DNS server) | `javascript-api k6-x-dns resolve` |
| `lookup()` (system DNS) | `javascript-api k6-x-dns lookup` |

Supports A (IPv4) and AAAA (IPv6) records. Extension module — auto-resolved on import.
