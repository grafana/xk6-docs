# k6/execution

| I need to... | Path |
|---|---|
| Runtime info (current VU, scenario, iteration, test state) | `javascript-api k6-execution` |

Exposes `execution.vu`, `execution.scenario`, `execution.instance`, and `execution.test` objects.
