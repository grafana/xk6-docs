# k6/x/icmp

| I need to... | Path |
|---|---|
| ICMP ping API | `javascript-api k6-x-icmp` |
| Sync ping | `javascript-api k6-x-icmp ping` |
| Async ping | `javascript-api k6-x-icmp ping-async` |
| Ping options | `javascript-api k6-x-icmp ping-options` |
| Ping callback data structure | `javascript-api k6-x-icmp ping-detail` |

Extension module — auto-resolved on import. Useful for reachability checks and latency measurement.
